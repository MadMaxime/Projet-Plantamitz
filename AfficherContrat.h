#include <stdio.h>
#include <windows.h

typedef struct {
    int soleils;
    int fraises;
    int pommes;
    int oignons;
    int mandarines;
    int coupsMax;
} Contrat;

void afficherContrat(Contrat contrat, Contrat progression);
